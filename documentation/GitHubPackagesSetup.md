# GitHub Setup

Repository settings:

1. Set TOKEN_GITHUB_PACKAGES to a personal access token with the scopes "read:packages" and "write:packages".
2. Set CLIENT_SECRET_AZURE to the client secret of the app registration for updating Azure Container Apps (obtained from Microsoft Entra).

Package settings:

1. Link package to repository
2. Change package visibility to public
